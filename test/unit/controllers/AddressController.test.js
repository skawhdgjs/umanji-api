import { assert } from 'chai';
import Controller from '../../../api/controllers/AddressController';

describe('controllers:AddressController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
