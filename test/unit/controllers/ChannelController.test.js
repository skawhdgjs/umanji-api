import { assert } from 'chai';
import Controller from '../../../api/controllers/ChannelController';

describe('controllers:ChannelController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
