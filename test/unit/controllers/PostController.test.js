import { assert } from 'chai';
import Controller from '../../../api/controllers/PostController';

describe('controllers:PostController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
