import { assert } from 'chai';
import Model from '../../../api/models/Address';
import Fixture from '../../fixtures/AddressFixture';

describe('models:Address', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
