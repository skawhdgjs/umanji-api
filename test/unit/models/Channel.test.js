import { assert } from 'chai';
import Model from '../../../api/models/Channel';
import Fixture from '../../fixtures/ChannelFixture';

describe('models:Channel', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
