import { assert } from 'chai';
import Model from '../../../api/models/Post';
import Fixture from '../../fixtures/PostFixture';

describe('models:Post', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
